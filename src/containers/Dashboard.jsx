import React from 'react'
import { Layout } from '../components';

const Dashboard = () => {
    return (
        <Layout>
            Dashboard
        </Layout>
    )
}

export default Dashboard
